import SwiftUI

struct ReportsView: View {
    var body: some View {
        PlaceholderScreen(
            title: "Reports",
            message: "Pay reports and export tools will appear here.",
            systemImage: "doc.text"
        )
    }
}
