import SwiftUI

struct DashboardView: View {
    var body: some View {
        PlaceholderScreen(
            title: "Dashboard",
            message: "Your shift summary and pay snapshot will appear here.",
            systemImage: "gauge"
        )
    }
}
