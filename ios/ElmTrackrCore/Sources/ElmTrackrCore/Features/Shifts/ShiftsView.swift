import SwiftUI

struct ShiftsView: View {
    var body: some View {
        PlaceholderScreen(
            title: "Shifts",
            message: "Shift entry and editing will appear here.",
            systemImage: "clock"
        )
    }
}
