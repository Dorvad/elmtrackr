import SwiftUI

struct SettingsView: View {
    var body: some View {
        PlaceholderScreen(
            title: "Settings",
            message: "App preferences will appear here.",
            systemImage: "gearshape"
        )
    }
}
