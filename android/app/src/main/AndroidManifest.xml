<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required for notifications on Android 13+ (API 33+). Runtime-requested in MainActivity. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".ElmTrackrApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ElmTrackr">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep-link handler for Supabase auth callbacks.
                 Redirect URL to configure in Supabase dashboard:
                 elmtrackr://auth/callback
                 Handles: email confirmation, password reset, magic link, OAuth. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="elmtrackr"
                    android:host="auth" />
            </intent-filter>

            <!-- App shortcuts: static "New Manual Shift" + dynamic Clock In/Out -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- Handles "Clock Out" action from the active-shift notification. -->
        <receiver
            android:name=".notification.ClockOutReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.elmtrackr.app.ACTION_CLOCK_OUT" />
            </intent-filter>
        </receiver>

        <!-- ElmTrackr home screen widget (Jetpack Glance 4×1 horizontal). -->
        <receiver
            android:name=".widget.ElmTrackrWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/elmtrackr_widget_info" />
        </receiver>

    </application>

</manifest>
