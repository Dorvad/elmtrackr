<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Required for notifications on Android 13+ (API 33+). Runtime-requested in MainActivity. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- Restores the ongoing active-shift notification after a reboot. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <application
        android:name=".ElmTrackrApp"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ElmTrackr">

        <meta-data
            android:name="io.sentry.auto-init"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:enableOnBackInvokedCallback="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep-link handler for Supabase auth callbacks.
                 Redirect URLs to configure in Supabase dashboard:
                 elmtrackr://auth/callback
                 elmtrackr://auth/reset-password -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="elmtrackr"
                    android:host="auth" />
            </intent-filter>

            <!-- App shortcuts: static "New Manual Shift" + dynamic Clock Out while clocked in. -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>


        <activity
            android:name=".shortcuts.HeadlessTrampolineActivity"
            android:theme="@android:style/Theme.NoDisplay"
            android:exported="false"
            android:noHistory="true"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <receiver
            android:name=".shortcuts.HeadlessClockOutReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.elmtrackr.ACTION_HEADLESS_CLOCK_OUT" />
            </intent-filter>
        </receiver>
        <!-- Handles "Clock Out" action from the active-shift notification. -->
        <receiver
            android:name=".notification.ClockOutReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.elmtrackr.app.ACTION_CLOCK_OUT" />
            </intent-filter>
        </receiver>

        <!-- Repaints idle widgets when the day/clock/timezone changes so the
             date label and day total never go stale overnight. -->
        <receiver
            android:name=".widget.WidgetDateChangeReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.DATE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".startup.BootCompletedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- ElmTrackr home screen widgets (Jetpack Glance 4×1 horizontal). -->
        <receiver
            android:name=".widget.ElmTrackrWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_label_classic">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/elmtrackr_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.ElmTrackrMinimalWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_label_minimal">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/elmtrackr_widget_minimal_info" />
        </receiver>

        <receiver
            android:name=".widget.ElmTrackrAuroraWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_label_aurora">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/elmtrackr_widget_aurora_info" />
        </receiver>

        <receiver
            android:name=".widget.ElmTrackrRingWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_label_ring">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/elmtrackr_widget_ring_info" />
        </receiver>

        <receiver
            android:name=".widget.ElmTrackrBigActionWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_label_big_action">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/elmtrackr_widget_big_action_info" />
        </receiver>

        <!-- Persists the in-app language choice on Android 12 and below
             (per-app locales via AppCompatDelegate.setApplicationLocales). -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/export_paths" />
        </provider>

        <service
            android:name=".wear.WearMessageListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:host="*"
                    android:pathPrefix="/elmtrackr/"
                    android:scheme="wear" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>

