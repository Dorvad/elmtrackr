<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Black first frame (WO-V13 / WO-V15). Theme.DeviceDefault on some Wear
         images flashes a light windowBackground before Compose draws, which a
         reviewer records as a crash or as "not a black background". -->
    <style name="Theme.ElmTrackrWear" parent="@android:style/Theme.DeviceDefault">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>
</resources>
