<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- API 31+ splash (WO-V15):  launcher icon on a black window. A missing
         splash theme on Wear OS 4/5 has been observed to throw while the
         platform injects the splash before the first activity frame. -->
    <style name="Theme.ElmTrackrWear" parent="@android:style/Theme.DeviceDefault">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowSplashScreenBackground">@android:color/black</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/black</item>
    </style>
</resources>
