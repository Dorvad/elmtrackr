<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- The running shift is surfaced as a Wear OS Ongoing Activity, which rides on
         an ongoing notification. Runtime-requested in WearMainActivity on API 33+;
         without it the watch face indicator and the recents chip never appear. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".ElmTrackrWearApp"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ElmTrackrWear">

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <!-- Sentry initialises from WearCrashReporting and nowhere else.

             sentry-android-core contributes two content providers,
             SentryInitProvider and SentryPerformanceProvider, and a content
             provider runs before Application.onCreate — which means before the
             guard WearCrashReporting puts around init. In a module Play keeps
             rejecting for dying on the launch path, unguarded library code
             ahead of our own is exactly what we cannot add. This is Sentry's
             documented switch for manual initialisation: the providers stay
             installed (the SDK reads app-start timing from one of them) and
             skip auto-init, so the only place the SDK starts is inside a
             runCatching we own. -->
        <meta-data
            android:name="io.sentry.auto-init"
            android:value="false" />

        <!-- WorkManager and ProfileInstaller both register androidx.startup
             initializers, which are ContentProviders and therefore run before
             Application.onCreate. :app already removes WorkManager's. The watch
             did not, so a JobScheduler failure on a review device died on the
             launch path before WearCrashReporting could start. On-demand init
             is Configuration.Provider on ElmTrackrWearApp. ProfileInstaller is
             a compile-time dex thing; it has no business running on a wrist
             being reviewed for launch crashes. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
            <meta-data
                android:name="androidx.profileinstaller.ProfileInstallerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />

        <activity
            android:name=".WearMainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@style/Theme.ElmTrackrWear">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The tile's punch button targets this activity through a
             LaunchAction. That action is carried out by the Wear OS tile host,
             which is a separate app under a separate uid, so the activity has
             to be exported for the tap to reach it at all — with
             exported="false" the activity manager refuses the start and the
             tile's one-tap punch silently does nothing. It stays invisible,
             finishes in onCreate, and honours only the two punch actions. -->
        <activity
            android:name=".tile.WearPunchTrampolineActivity"
            android:exported="true"
            android:noHistory="true"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <service
            android:name=".sync.WearDataListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:host="*"
                    android:pathPrefix="/elmtrackr/"
                    android:scheme="wear" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:host="*"
                    android:pathPrefix="/elmtrackr/"
                    android:scheme="wear" />
            </intent-filter>
            <!-- Phone reconnect: drain wrist punches that were queued while
                 unpaired, even if the user never opens the watch launcher. -->
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.CAPABILITY_CHANGED" />
                <data
                    android:host="*"
                    android:pathPrefix="/"
                    android:scheme="wear" />
            </intent-filter>
        </service>

        <service
            android:name=".tile.ElmTrackrTileService"
            android:exported="true"
            android:label="@string/tile_label"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@drawable/tile_preview" />
        </service>

        <service
            android:name=".complication.ElmTrackrComplicationService"
            android:exported="true"
            android:icon="@drawable/ic_complication"
            android:label="@string/complication_label"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT,LONG_TEXT,RANGED_VALUE" />
            <!-- 0 = no periodic polling: the phone pushes snapshots and the
                 sync layer calls requestUpdateAll() on every change. -->
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="0" />
        </service>

    </application>

</manifest>
